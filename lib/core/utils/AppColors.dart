import 'package:flutter/material.dart';

class AppColors {
  //Define the colors using in the app
  static const Color appLightBackground = Color.fromARGB(255, 225, 213, 201);
  static const Color appLightFontColor = Color.fromARGB(255, 0, 0, 0);
  static const Color appRedColor = Color.fromARGB(255, 178, 0, 0);
}
